public class SnailTwo
{
   public static int[] Snail(int[][] array)
   {
      var snailList = new List<int>();
      var totalRows = array.Length;
      var totalCols = array.GetLength(0);

      int currentRow = 0;
      int currentCol = 0;


      return [0, 0];

   }

}